<p id="notice"><%= notice %></p>

<h1>Comensales</h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Codigo</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @comensales.each do |comensale| %>
      <tr>
        <td><%= comensale.nombre %></td>
        <td><%= comensale.codigo %></td>
        <td><%= link_to 'Show', comensale %></td>
        <td><%= link_to 'Edit', edit_comensale_path(comensale) %></td>
        <td><%= link_to 'Destroy', comensale, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Comensale', new_comensale_path %>
